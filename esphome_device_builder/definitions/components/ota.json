{"id":"ota","name":"OTA Updates","description":"Over-the-Air firmware updates. When `ota:` is configured as a bare mapping (no `- platform:` list — the legacy form), ESPHome implicitly uses the `esphome` platform. Modern configs select a platform explicitly: available platforms are `esphome`, `http_request`, `web_server`, `zephyr_mcumgr`.","category":"ota","docs_url":"https://esphome.io/components/ota/","image_url":"https://esphome.io/images/system-update.svg"}
